const BackLight = () => {
    return (
        <div className="bg-linear-to-t from-background to-secondary/30 blur-3xl left-1/2 -translate-x-1/2 w-[max(100vw,770px)] h-[max(100vw,770px)] absolute z-1 rounded-full top-[30%] animate-pulse [animation-duration:4s]" />
    )
}

export default BackLight